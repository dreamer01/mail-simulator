import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import { Carousel } from 'react-responsive-carousel';

export class ImgPack extends Component {
    render() {
        return (
            <div> Images </div>
        );
    }
}